# Development-only pg_hba example.
# Do not mount this file in production.
#
# The previous version allowed trust authentication from 0.0.0.0/0. Keep this
# sample password-based so accidental use fails closed instead of opening the DB.
local   all             all                                     scram-sha-256
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256
local   replication     all                                     scram-sha-256
host    replication     all             127.0.0.1/32            scram-sha-256
host    replication     all             ::1/128                 scram-sha-256
